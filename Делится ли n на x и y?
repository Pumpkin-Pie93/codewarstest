// nСоздайте функцию, которая проверяет, делится ли число на два числа x AND y . Все входные данные являются положительными, ненулевыми числами.

function isDivisible(n, x, y) {
  return (n % x == 0 && n % y ==0)? true: false;
}
